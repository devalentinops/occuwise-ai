# ---- Core DL stack ----
# NOTE: install a CUDA-matched torch build from https://pytorch.org first, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.3,<3.0
torchvision>=0.18
pytorch-lightning>=2.3,<3.0
torchmetrics>=1.4

# ---- Model zoos ----
timm>=1.0.7                     # ResNet / EfficientNet / DenseNet / ViT backbones + HF-hub loading
huggingface-hub>=0.24          # fetch domain-pretrained checkpoints (RETFound) — also a timm dep
segmentation-models-pytorch>=0.3.4   # U-Net (+ many encoders) for segmentation
monai>=1.3                      # medical-imaging transforms, losses, metrics (optional but recommended)

# ---- Config / experiment management ----
hydra-core>=1.3
omegaconf>=2.3
mlflow>=2.14,<3                 # experiment tracking + model registry (audit trail).
                               # Pinned <3: MLflow 3.x deprecates the simple ./mlruns file
                               # store this project uses; 2.x keeps it as documented.
# wandb>=0.17                   # optional alternative tracker

# ---- Data / augmentation ----
albumentations>=1.4
opencv-python-headless>=4.9
pillow>=10.3
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
scikit-image>=0.23

# ---- Explainability (important for clinical trust) ----
grad-cam>=1.5                   # pytorch-grad-cam: Grad-CAM/++/ScoreCAM saliency

# ---- Export / serving ----
onnx>=1.16
onnxruntime>=1.18              # use onnxruntime-gpu on the serving host
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # file uploads
pydicom>=2.4                   # DICOM ingestion (production imaging)

# ---- Tooling ----
rich>=13.7
tqdm>=4.66
matplotlib>=3.9
seaborn>=0.13
pytest>=8.2
ruff>=0.5
